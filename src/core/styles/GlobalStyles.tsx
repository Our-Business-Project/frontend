import { GlobalStyles as GS } from '@mui/material';

const GlobalStyles = () => {
  return (
    <GS
      styles={{
        a: {
          color: '#FFFFFF',
        },
      }}
    />
  );
};

export default GlobalStyles;
